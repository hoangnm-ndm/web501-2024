import React from "react";

const HomePage = ({ data }) => {
	console.log(data);
	return (
		<>
			{/* {data.map((item) => (
				<div key={item.id}>
					<h2>{item.name}</h2>
					<p>{item.price}</p>
				</div>
			))} */}
		</>
	);
};

export default HomePage;
